import React from "react";
import {StyleSheet, Text, View} from 'react-native'

const LancamentoItem = () => {
    return(
        <View>
            <Text>LancamentoItem</Text>
        </View>
    );
};

const styles = StyleSheet.create({
   
});

export default LancamentoItem;